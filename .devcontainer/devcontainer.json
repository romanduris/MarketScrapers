{
  "name": "MarketScrapers",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "customizations": {
    "vscode": {
      "extensions": ["ms-python.python"]
    }
  },
  "remoteEnv": {
    "OPENAI_API_KEY": "${localEnv:OPENAI_API_KEY}",
    "EMAIL_PASS": "${localEnv:EMAIL_PASS}"
  },
  "postStartCommand": "echo '✅ Devcontainer initialized successfully'"
}
